name: CI-CD Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH key
        env:
          EC2_KEY: ${{ secrets.EC2_KEY }}
        run: |
          mkdir -p ~/.ssh

          python3 - <<'PY'
          import os
          import base64
          from pathlib import Path

          key = os.environ["EC2_KEY"].strip().replace("\\n", "\n").replace("\r", "")
          if "BEGIN OPENSSH PRIVATE KEY" not in key and "BEGIN RSA PRIVATE KEY" not in key and "BEGIN PRIVATE KEY" not in key:
              key = base64.b64decode(key).decode()

          key_path = Path.home() / ".ssh" / "key.pem"
          key_path.write_text(key)
          key_path.chmod(0o600)
          PY

          ssh-keygen -y -f ~/.ssh/key.pem >/dev/null

          # prevent host prompt issues
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        run: |
          ssh -i ~/.ssh/key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'

          set -e

          echo "=== Deployment started ==="

          cd /app/knitt

          echo "Pulling latest code..."
          git pull origin main

          echo "Building and restarting containers..."

          docker compose -f infra/docker-compose.prod.yaml up -d --build

          echo "Cleaning unused Docker resources..."
          docker system prune -f

          echo "=== Deployment successful ==="

          EOF